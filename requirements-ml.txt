# Необязательные тяжёлые модели. Ставятся поверх requirements.txt и включаются
# автоматически, как только становятся доступны.
#
# На сервере без видеокарты ставьте CPU-сборку torch — иначе pip притащит
# 3.2 ГБ библиотек CUDA, которые никогда не понадобятся:
#   pip install --index-url https://download.pytorch.org/whl/cpu \
#               --extra-index-url https://pypi.org/simple -r requirements-ml.txt
#
# Что даёт каждый пакет:
#   demucs         — студийное разделение на дорожки вместо DSP-метода
#   faster-whisper — расшифровка текста песни с таймкодами
#   anthropic      — уточнение аранжировки по описанию через LLM (нужен ANTHROPIC_API_KEY)
#   librosa        — альтернативные алгоритмы анализа
-r requirements.txt
torch>=2.2
demucs>=4.0
faster-whisper>=1.0
anthropic>=0.40
librosa>=0.10
